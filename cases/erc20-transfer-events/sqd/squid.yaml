manifestVersion: subsquid.io/v0.1
name: erc20-transfer-events
version: 1
description: 'ERC20 Transfer Events benchmark squid'
build:
deploy:
  addons:
    postgres:
    rpc:
      - eth.http
  processor:
    cmd:
      - sqd
      - process:prod
  api:
    cmd:
      - sqd
      - serve:prod
