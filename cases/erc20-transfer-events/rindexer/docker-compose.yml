volumes:
  postgres_data:
    driver: local

services:
  postgresql:
    image: postgres:18
    shm_size: 1g
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql
    ports:
      - 5440:5432
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-rindexer}
