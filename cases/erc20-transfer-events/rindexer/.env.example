# Ethereum RPC endpoint
ETHEREUM_RPC=https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY

# PostgreSQL connection (used by docker-compose and rindexer)
POSTGRES_PASSWORD=rindexer
DATABASE_URL=postgresql://postgres:rindexer@localhost:5440/postgres

# Data source. The RPC row runs hypersync=false with the pinned range/fan-out;
# the HyperSync row runs hypersync=true with rindexer's defaults and reads its
# API token from ENVIO_API_TOKEN (https://envio.dev/app/api-tokens).
RINDEXER_HYPERSYNC=false
RINDEXER_MAX_BLOCK_RANGE=1000
RINDEXER_FETCH_CONCURRENCY=10
# ENVIO_API_TOKEN=your-token
