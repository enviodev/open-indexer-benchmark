#![allow(
    non_camel_case_types,
    clippy::enum_variant_names,
    clippy::too_many_arguments,
    clippy::upper_case_acronyms,
    clippy::type_complexity,
    dead_code
)]
// THIS IS A GENERATED FILE. DO NOT MODIFY MANUALLY.
//
// This file was auto generated by rindexer - https://github.com/joshstevens19/rindexer.
// Any manual changes to this file will be overwritten.

use super::super::super::super::typings::database::get_or_init_postgres_client;
use super::super::super::super::typings::networks::get_provider_cache_for_network;
use super::child_common_v14_abi_gen::RindexerChildCommonV14Gen::{
    self, RindexerChildCommonV14GenEvents, RindexerChildCommonV14GenInstance,
};
use alloy::primitives::{Address, Bytes, B256};
use alloy::sol_types::{SolEvent, SolEventInterface, SolType};
use rindexer::{
    async_trait,
    blockclock::BlockClock,
    event::{
        callback_registry::{
            EventCallbackRegistry, EventCallbackRegistryInformation, EventCallbackResult,
            EventResult, HasTxInformation, TxInformation,
        },
        contract_setup::{ContractInformation, NetworkContract},
    },
    generate_random_id,
    manifest::{
        contract::{Contract, ContractDetails},
        yaml::read_manifest,
    },
    provider::{ChainProvider, JsonRpcCachedProvider},
    FutureExt, PostgresClient,
};
use std::collections::HashMap;
use std::error::Error;
use std::future::Future;
use std::path::{Path, PathBuf};
use std::pin::Pin;
use std::{any::Any, sync::Arc};

pub type SafeSetupData = RindexerChildCommonV14Gen::SafeSetup;

#[derive(Debug, Clone)]
pub struct SafeSetupResult {
    pub event_data: SafeSetupData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for SafeSetupResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type SafeReceivedData = RindexerChildCommonV14Gen::SafeReceived;

#[derive(Debug, Clone)]
pub struct SafeReceivedResult {
    pub event_data: SafeReceivedData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for SafeReceivedResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type SafeModuleTransactionData = RindexerChildCommonV14Gen::SafeModuleTransaction;

#[derive(Debug, Clone)]
pub struct SafeModuleTransactionResult {
    pub event_data: SafeModuleTransactionData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for SafeModuleTransactionResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type SafeMultiSigTransactionData = RindexerChildCommonV14Gen::SafeMultiSigTransaction;

#[derive(Debug, Clone)]
pub struct SafeMultiSigTransactionResult {
    pub event_data: SafeMultiSigTransactionData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for SafeMultiSigTransactionResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type ChangedThresholdData = RindexerChildCommonV14Gen::ChangedThreshold;

#[derive(Debug, Clone)]
pub struct ChangedThresholdResult {
    pub event_data: ChangedThresholdData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for ChangedThresholdResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

type BoxFuture<'a, T> = Pin<Box<dyn Future<Output = T> + Send + 'a>>;

#[async_trait]
trait EventCallback {
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()>;
}

pub struct EventContext<TExtensions>
where
    TExtensions: Send + Sync,
{
    pub database: Arc<PostgresClient>,

    pub extensions: Arc<TExtensions>,
    reorg_tx: tokio::sync::broadcast::Sender<rindexer::ReorgEvent>,
}

impl<TExtensions> EventContext<TExtensions>
where
    TExtensions: Send + Sync,
{
    /// Subscribe to reorg events. Returns a receiver that will get notified
    /// whenever a reorg is detected and recovery is complete.
    pub fn reorg_receiver(&self) -> tokio::sync::broadcast::Receiver<rindexer::ReorgEvent> {
        self.reorg_tx.subscribe()
    }
}

// didn't want to use option or none made harder DX
// so a blank struct makes interface nice
pub struct NoExtensions {}
pub fn no_extensions() -> NoExtensions {
    NoExtensions {}
}

pub fn safesetup_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> SafeSetupEventCallbackType<TExtensions>
where
    SafeSetupResult: Clone + 'static,
    F: for<'a> Fn(Vec<SafeSetupResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type SafeSetupEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<SafeSetupResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct SafeSetupEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: SafeSetupEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> SafeSetupEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        SafeSetupResult: Clone + 'static,
        F: for<'a> Fn(Vec<SafeSetupResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: safesetup_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for SafeSetupEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<SafeSetupResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<SafeSetupData>()
                    .ok()
                    .map(|arc| SafeSetupResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        if result.len() == events_len {
            (self.callback)(&result, Arc::clone(&self.context)).await
        } else {
            panic!("SafeSetupEvent: Unexpected data type - expected: SafeSetupData")
        }
    }
}

pub fn safereceived_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> SafeReceivedEventCallbackType<TExtensions>
where
    SafeReceivedResult: Clone + 'static,
    F: for<'a> Fn(Vec<SafeReceivedResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type SafeReceivedEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<SafeReceivedResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct SafeReceivedEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: SafeReceivedEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> SafeReceivedEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        SafeReceivedResult: Clone + 'static,
        F: for<'a> Fn(Vec<SafeReceivedResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: safereceived_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for SafeReceivedEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<SafeReceivedResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<SafeReceivedData>()
                    .ok()
                    .map(|arc| SafeReceivedResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        if result.len() == events_len {
            (self.callback)(&result, Arc::clone(&self.context)).await
        } else {
            panic!("SafeReceivedEvent: Unexpected data type - expected: SafeReceivedData")
        }
    }
}

pub fn safemoduletransaction_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> SafeModuleTransactionEventCallbackType<TExtensions>
where
    SafeModuleTransactionResult: Clone + 'static,
    F: for<'a> Fn(Vec<SafeModuleTransactionResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type SafeModuleTransactionEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<SafeModuleTransactionResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct SafeModuleTransactionEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: SafeModuleTransactionEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> SafeModuleTransactionEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        SafeModuleTransactionResult: Clone + 'static,
        F: for<'a> Fn(Vec<SafeModuleTransactionResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: safemoduletransaction_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for SafeModuleTransactionEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<SafeModuleTransactionResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<SafeModuleTransactionData>()
                    .ok()
                    .map(|arc| SafeModuleTransactionResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        if result.len() == events_len {
            (self.callback)(&result, Arc::clone(&self.context)).await
        } else {
            panic!("SafeModuleTransactionEvent: Unexpected data type - expected: SafeModuleTransactionData")
        }
    }
}

pub fn safemultisigtransaction_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> SafeMultiSigTransactionEventCallbackType<TExtensions>
where
    SafeMultiSigTransactionResult: Clone + 'static,
    F: for<'a> Fn(Vec<SafeMultiSigTransactionResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type SafeMultiSigTransactionEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<SafeMultiSigTransactionResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct SafeMultiSigTransactionEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: SafeMultiSigTransactionEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> SafeMultiSigTransactionEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        SafeMultiSigTransactionResult: Clone + 'static,
        F: for<'a> Fn(Vec<SafeMultiSigTransactionResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: safemultisigtransaction_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for SafeMultiSigTransactionEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<SafeMultiSigTransactionResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<SafeMultiSigTransactionData>()
                    .ok()
                    .map(|arc| SafeMultiSigTransactionResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        if result.len() == events_len {
            (self.callback)(&result, Arc::clone(&self.context)).await
        } else {
            panic!("SafeMultiSigTransactionEvent: Unexpected data type - expected: SafeMultiSigTransactionData")
        }
    }
}

pub fn changedthreshold_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> ChangedThresholdEventCallbackType<TExtensions>
where
    ChangedThresholdResult: Clone + 'static,
    F: for<'a> Fn(Vec<ChangedThresholdResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type ChangedThresholdEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<ChangedThresholdResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct ChangedThresholdEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: ChangedThresholdEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> ChangedThresholdEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        ChangedThresholdResult: Clone + 'static,
        F: for<'a> Fn(Vec<ChangedThresholdResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: changedthreshold_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for ChangedThresholdEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<ChangedThresholdResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<ChangedThresholdData>()
                    .ok()
                    .map(|arc| ChangedThresholdResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        if result.len() == events_len {
            (self.callback)(&result, Arc::clone(&self.context)).await
        } else {
            panic!("ChangedThresholdEvent: Unexpected data type - expected: ChangedThresholdData")
        }
    }
}

pub enum ChildCommonV14EventType<TExtensions>
where
    TExtensions: 'static + Send + Sync,
{
    SafeSetup(SafeSetupEvent<TExtensions>),
    SafeReceived(SafeReceivedEvent<TExtensions>),
    SafeModuleTransaction(SafeModuleTransactionEvent<TExtensions>),
    SafeMultiSigTransaction(SafeMultiSigTransactionEvent<TExtensions>),
    ChangedThreshold(ChangedThresholdEvent<TExtensions>),
}

impl<TExtensions> ChildCommonV14EventType<TExtensions>
where
    TExtensions: 'static + Send + Sync,
{
    pub fn topic_id(&self) -> &'static str {
        match self {
            ChildCommonV14EventType::SafeSetup(_) => {
                "0x141df868a6331af528e38c83b7aa03edc19be66e37ae67f9285bf4f8e3c6a1a8"
            }
            ChildCommonV14EventType::SafeReceived(_) => {
                "0x3d0ce9bfc3ed7d6862dbb28b2dea94561fe714a1b4d019aa8af39730d1ad7c3d"
            }
            ChildCommonV14EventType::SafeModuleTransaction(_) => {
                "0xb648d3644f584ed1c2232d53c46d87e693586486ad0d1175f8656013110b714e"
            }
            ChildCommonV14EventType::SafeMultiSigTransaction(_) => {
                "0x66753cd2356569ee081232e3be8909b950e0a76c1f8460c3a5e3c2be32b11bed"
            }
            ChildCommonV14EventType::ChangedThreshold(_) => {
                "0x610f7ff2b304ae8903c3de74c60c6ab1f7d6226b3f52c5161905bb5ad4039c93"
            }
        }
    }

    pub fn event_name(&self) -> &'static str {
        match self {
            ChildCommonV14EventType::SafeSetup(_) => "SafeSetup",
            ChildCommonV14EventType::SafeReceived(_) => "SafeReceived",
            ChildCommonV14EventType::SafeModuleTransaction(_) => "SafeModuleTransaction",
            ChildCommonV14EventType::SafeMultiSigTransaction(_) => "SafeMultiSigTransaction",
            ChildCommonV14EventType::ChangedThreshold(_) => "ChangedThreshold",
        }
    }

    pub fn contract_name(&self) -> String {
        "ChildCommonV14".to_string()
    }

    async fn get_provider(&self, network: &str) -> Arc<dyn ChainProvider> {
        get_provider_cache_for_network(network).await
    }

    fn decoder(
        &self,
        network: &str,
    ) -> Arc<dyn Fn(Vec<B256>, Bytes) -> Arc<dyn Any + Send + Sync> + Send + Sync> {
        match self {
            ChildCommonV14EventType::SafeSetup(_) => Arc::new(
                move |topics: Vec<B256>, data: Bytes| match SafeSetupData::decode_raw_log(
                    topics,
                    &data[0..],
                ) {
                    Ok(event) => {
                        let result: SafeSetupData = event;
                        Arc::new(result) as Arc<dyn Any + Send + Sync>
                    }
                    Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                },
            ),

            ChildCommonV14EventType::SafeReceived(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match SafeReceivedData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: SafeReceivedData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }

            ChildCommonV14EventType::SafeModuleTransaction(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match SafeModuleTransactionData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: SafeModuleTransactionData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }

            ChildCommonV14EventType::SafeMultiSigTransaction(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match SafeMultiSigTransactionData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: SafeMultiSigTransactionData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }

            ChildCommonV14EventType::ChangedThreshold(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match ChangedThresholdData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: ChangedThresholdData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }
        }
    }

    pub async fn register(self, manifest_path: &PathBuf, registry: &mut EventCallbackRegistry) {
        let rindexer_yaml = read_manifest(manifest_path).expect("Failed to read rindexer.yaml");
        let topic_id = self.topic_id();
        let contract_name = self.contract_name();
        let event_name = self.event_name();

        let contract_details = rindexer_yaml
            .all_contracts()
            .iter()
            .find(|c| c.name == contract_name)
            .unwrap_or_else(|| {
                panic!(
                    "Contract {} not found please make sure its defined in the rindexer.yaml",
                    contract_name
                )
            })
            .clone();

        let index_event_in_order = contract_details
            .index_event_in_order
            .as_ref()
            .is_some_and(|vec| vec.contains(&event_name.to_string()));

        // Expect providers to have been initialized, but it's an async init so this should
        // be fast but for correctness we must await each future.
        let mut providers = HashMap::new();
        for n in contract_details.details.iter() {
            let provider: Arc<dyn ChainProvider> = self.get_provider(&n.network).await;
            providers.insert(n.network.clone(), provider);
        }

        let contract = ContractInformation {
            name: contract_details
                .before_modify_name_if_filter_readonly()
                .into_owned(),
            details: contract_details
                .details
                .iter()
                .map(|c| {
                    let provider = providers
                        .get(&c.network)
                        .expect("must have a provider")
                        .clone();

                    NetworkContract {
                        id: generate_random_id(10),
                        network: c.network.clone(),
                        cached_provider: provider.clone(),
                        block_clock: BlockClock::new(
                            rindexer_yaml.timestamps,
                            rindexer_yaml.config.timestamp_sample_rate,
                            provider.clone(),
                        ),
                        decoder: self.decoder(&c.network),
                        indexing_contract_setup: c.indexing_contract_setup(manifest_path),
                        start_block: c.start_block,
                        end_block: c.end_block,
                        disable_logs_bloom_checks: rindexer_yaml
                            .networks
                            .iter()
                            .find(|n| n.name == c.network)
                            .is_some_and(|n| n.disable_logs_bloom_checks.unwrap_or_default()),
                    }
                })
                .collect(),
            abi: contract_details.abi,
            reorg_safe_distance: contract_details.reorg_safe_distance,
        };

        let (callback, reorg_sender): (
            Arc<
                dyn Fn(Vec<EventResult>) -> BoxFuture<'static, EventCallbackResult<()>>
                    + Send
                    + Sync,
            >,
            Option<tokio::sync::broadcast::Sender<rindexer::ReorgEvent>>,
        ) = match self {
            ChildCommonV14EventType::SafeSetup(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildCommonV14EventType::SafeReceived(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildCommonV14EventType::SafeModuleTransaction(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildCommonV14EventType::SafeMultiSigTransaction(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildCommonV14EventType::ChangedThreshold(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }
        };

        registry.register_event(EventCallbackRegistryInformation {
            id: generate_random_id(10),
            indexer_name: "safe-indexer".to_string(),
            event_name: event_name.to_string(),
            index_event_in_order,
            topic_id: topic_id.parse::<B256>().unwrap(),
            contract,
            callback,
            tables: Arc::new(vec![]),
            reorg_sender,
            streams_clients: Arc::new(None),
            providers: Arc::new(providers),
            constants: Arc::new(rindexer_yaml.constants.clone()),
            multicall_addresses: Arc::new(
                rindexer_yaml
                    .networks
                    .iter()
                    .map(|n| (n.name.clone(), n.multicall3_address.clone()))
                    .collect(),
            ),
        });
    }
}
