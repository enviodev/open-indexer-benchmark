#![allow(
    non_camel_case_types,
    clippy::enum_variant_names,
    clippy::too_many_arguments,
    clippy::upper_case_acronyms,
    clippy::type_complexity,
    dead_code
)]
// THIS IS A GENERATED FILE. DO NOT MODIFY MANUALLY.
//
// This file was auto generated by rindexer - https://github.com/joshstevens19/rindexer.
// Any manual changes to this file will be overwritten.

use super::super::super::super::typings::database::get_or_init_postgres_client;
use super::super::super::super::typings::networks::get_provider_cache_for_network;
use super::child_legacy_layout_v13_abi_gen::RindexerChildLegacyLayoutV13Gen::{
    self, RindexerChildLegacyLayoutV13GenEvents, RindexerChildLegacyLayoutV13GenInstance,
};
use alloy::primitives::{Address, Bytes, B256};
use alloy::sol_types::{SolEvent, SolEventInterface, SolType};
use rindexer::{
    async_trait,
    blockclock::BlockClock,
    event::{
        callback_registry::{
            EventCallbackRegistry, EventCallbackRegistryInformation, EventCallbackResult,
            EventResult, HasTxInformation, TxInformation,
        },
        contract_setup::{ContractInformation, NetworkContract},
    },
    generate_random_id,
    manifest::{
        contract::{Contract, ContractDetails},
        yaml::read_manifest,
    },
    provider::{ChainProvider, JsonRpcCachedProvider},
    FutureExt, PostgresClient,
};
use std::collections::HashMap;
use std::error::Error;
use std::future::Future;
use std::path::{Path, PathBuf};
use std::pin::Pin;
use std::{any::Any, sync::Arc};

pub type ExecutionSuccessData = RindexerChildLegacyLayoutV13Gen::ExecutionSuccess;

#[derive(Debug, Clone)]
pub struct ExecutionSuccessResult {
    pub event_data: ExecutionSuccessData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for ExecutionSuccessResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type ExecutionFailureData = RindexerChildLegacyLayoutV13Gen::ExecutionFailure;

#[derive(Debug, Clone)]
pub struct ExecutionFailureResult {
    pub event_data: ExecutionFailureData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for ExecutionFailureResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type ChangedMasterCopyData = RindexerChildLegacyLayoutV13Gen::ChangedMasterCopy;

#[derive(Debug, Clone)]
pub struct ChangedMasterCopyResult {
    pub event_data: ChangedMasterCopyData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for ChangedMasterCopyResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type ChangedFallbackHandlerData = RindexerChildLegacyLayoutV13Gen::ChangedFallbackHandler;

#[derive(Debug, Clone)]
pub struct ChangedFallbackHandlerResult {
    pub event_data: ChangedFallbackHandlerData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for ChangedFallbackHandlerResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type ChangedGuardData = RindexerChildLegacyLayoutV13Gen::ChangedGuard;

#[derive(Debug, Clone)]
pub struct ChangedGuardResult {
    pub event_data: ChangedGuardData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for ChangedGuardResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type ChangedModuleGuardData = RindexerChildLegacyLayoutV13Gen::ChangedModuleGuard;

#[derive(Debug, Clone)]
pub struct ChangedModuleGuardResult {
    pub event_data: ChangedModuleGuardData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for ChangedModuleGuardResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type EnabledModuleData = RindexerChildLegacyLayoutV13Gen::EnabledModule;

#[derive(Debug, Clone)]
pub struct EnabledModuleResult {
    pub event_data: EnabledModuleData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for EnabledModuleResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type DisabledModuleData = RindexerChildLegacyLayoutV13Gen::DisabledModule;

#[derive(Debug, Clone)]
pub struct DisabledModuleResult {
    pub event_data: DisabledModuleData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for DisabledModuleResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type AddedOwnerData = RindexerChildLegacyLayoutV13Gen::AddedOwner;

#[derive(Debug, Clone)]
pub struct AddedOwnerResult {
    pub event_data: AddedOwnerData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for AddedOwnerResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

pub type RemovedOwnerData = RindexerChildLegacyLayoutV13Gen::RemovedOwner;

#[derive(Debug, Clone)]
pub struct RemovedOwnerResult {
    pub event_data: RemovedOwnerData,
    pub tx_information: TxInformation,
}

impl HasTxInformation for RemovedOwnerResult {
    fn tx_information(&self) -> &TxInformation {
        &self.tx_information
    }
}

type BoxFuture<'a, T> = Pin<Box<dyn Future<Output = T> + Send + 'a>>;

#[async_trait]
trait EventCallback {
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()>;
}

pub struct EventContext<TExtensions>
where
    TExtensions: Send + Sync,
{
    pub database: Arc<PostgresClient>,

    pub extensions: Arc<TExtensions>,
    reorg_tx: tokio::sync::broadcast::Sender<rindexer::ReorgEvent>,
}

impl<TExtensions> EventContext<TExtensions>
where
    TExtensions: Send + Sync,
{
    /// Subscribe to reorg events. Returns a receiver that will get notified
    /// whenever a reorg is detected and recovery is complete.
    pub fn reorg_receiver(&self) -> tokio::sync::broadcast::Receiver<rindexer::ReorgEvent> {
        self.reorg_tx.subscribe()
    }
}

// didn't want to use option or none made harder DX
// so a blank struct makes interface nice
pub struct NoExtensions {}
pub fn no_extensions() -> NoExtensions {
    NoExtensions {}
}

pub fn executionsuccess_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> ExecutionSuccessEventCallbackType<TExtensions>
where
    ExecutionSuccessResult: Clone + 'static,
    F: for<'a> Fn(Vec<ExecutionSuccessResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type ExecutionSuccessEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<ExecutionSuccessResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct ExecutionSuccessEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: ExecutionSuccessEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> ExecutionSuccessEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        ExecutionSuccessResult: Clone + 'static,
        F: for<'a> Fn(Vec<ExecutionSuccessResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: executionsuccess_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for ExecutionSuccessEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<ExecutionSuccessResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<ExecutionSuccessData>()
                    .ok()
                    .map(|arc| ExecutionSuccessResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        // Hand-edited on top of the generated output: this contract is one of
        // two registrations sharing each event's topic0 (the 1.3.x and 1.4.x
        // layouts), so a batch legitimately mixes logs of both. The ones that
        // decoded here are processed; the rest fail decode by design and are
        // handled by the sibling layout's registration.
        let _ = events_len;
        (self.callback)(&result, Arc::clone(&self.context)).await
    }
}

pub fn executionfailure_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> ExecutionFailureEventCallbackType<TExtensions>
where
    ExecutionFailureResult: Clone + 'static,
    F: for<'a> Fn(Vec<ExecutionFailureResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type ExecutionFailureEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<ExecutionFailureResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct ExecutionFailureEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: ExecutionFailureEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> ExecutionFailureEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        ExecutionFailureResult: Clone + 'static,
        F: for<'a> Fn(Vec<ExecutionFailureResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: executionfailure_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for ExecutionFailureEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<ExecutionFailureResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<ExecutionFailureData>()
                    .ok()
                    .map(|arc| ExecutionFailureResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        // Hand-edited on top of the generated output: this contract is one of
        // two registrations sharing each event's topic0 (the 1.3.x and 1.4.x
        // layouts), so a batch legitimately mixes logs of both. The ones that
        // decoded here are processed; the rest fail decode by design and are
        // handled by the sibling layout's registration.
        let _ = events_len;
        (self.callback)(&result, Arc::clone(&self.context)).await
    }
}

pub fn changedmastercopy_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> ChangedMasterCopyEventCallbackType<TExtensions>
where
    ChangedMasterCopyResult: Clone + 'static,
    F: for<'a> Fn(Vec<ChangedMasterCopyResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type ChangedMasterCopyEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<ChangedMasterCopyResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct ChangedMasterCopyEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: ChangedMasterCopyEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> ChangedMasterCopyEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        ChangedMasterCopyResult: Clone + 'static,
        F: for<'a> Fn(Vec<ChangedMasterCopyResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: changedmastercopy_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for ChangedMasterCopyEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<ChangedMasterCopyResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<ChangedMasterCopyData>()
                    .ok()
                    .map(|arc| ChangedMasterCopyResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        // Hand-edited on top of the generated output: this contract is one of
        // two registrations sharing each event's topic0 (the 1.3.x and 1.4.x
        // layouts), so a batch legitimately mixes logs of both. The ones that
        // decoded here are processed; the rest fail decode by design and are
        // handled by the sibling layout's registration.
        let _ = events_len;
        (self.callback)(&result, Arc::clone(&self.context)).await
    }
}

pub fn changedfallbackhandler_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> ChangedFallbackHandlerEventCallbackType<TExtensions>
where
    ChangedFallbackHandlerResult: Clone + 'static,
    F: for<'a> Fn(Vec<ChangedFallbackHandlerResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type ChangedFallbackHandlerEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<ChangedFallbackHandlerResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct ChangedFallbackHandlerEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: ChangedFallbackHandlerEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> ChangedFallbackHandlerEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        ChangedFallbackHandlerResult: Clone + 'static,
        F: for<'a> Fn(Vec<ChangedFallbackHandlerResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: changedfallbackhandler_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for ChangedFallbackHandlerEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<ChangedFallbackHandlerResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<ChangedFallbackHandlerData>()
                    .ok()
                    .map(|arc| ChangedFallbackHandlerResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        // Hand-edited on top of the generated output: this contract is one of
        // two registrations sharing each event's topic0 (the 1.3.x and 1.4.x
        // layouts), so a batch legitimately mixes logs of both. The ones that
        // decoded here are processed; the rest fail decode by design and are
        // handled by the sibling layout's registration.
        let _ = events_len;
        (self.callback)(&result, Arc::clone(&self.context)).await
    }
}

pub fn changedguard_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> ChangedGuardEventCallbackType<TExtensions>
where
    ChangedGuardResult: Clone + 'static,
    F: for<'a> Fn(Vec<ChangedGuardResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type ChangedGuardEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<ChangedGuardResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct ChangedGuardEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: ChangedGuardEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> ChangedGuardEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        ChangedGuardResult: Clone + 'static,
        F: for<'a> Fn(Vec<ChangedGuardResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: changedguard_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for ChangedGuardEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<ChangedGuardResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<ChangedGuardData>()
                    .ok()
                    .map(|arc| ChangedGuardResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        // Hand-edited on top of the generated output: this contract is one of
        // two registrations sharing each event's topic0 (the 1.3.x and 1.4.x
        // layouts), so a batch legitimately mixes logs of both. The ones that
        // decoded here are processed; the rest fail decode by design and are
        // handled by the sibling layout's registration.
        let _ = events_len;
        (self.callback)(&result, Arc::clone(&self.context)).await
    }
}

pub fn changedmoduleguard_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> ChangedModuleGuardEventCallbackType<TExtensions>
where
    ChangedModuleGuardResult: Clone + 'static,
    F: for<'a> Fn(Vec<ChangedModuleGuardResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type ChangedModuleGuardEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<ChangedModuleGuardResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct ChangedModuleGuardEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: ChangedModuleGuardEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> ChangedModuleGuardEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        ChangedModuleGuardResult: Clone + 'static,
        F: for<'a> Fn(Vec<ChangedModuleGuardResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: changedmoduleguard_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for ChangedModuleGuardEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<ChangedModuleGuardResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<ChangedModuleGuardData>()
                    .ok()
                    .map(|arc| ChangedModuleGuardResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        // Hand-edited on top of the generated output: this contract is one of
        // two registrations sharing each event's topic0 (the 1.3.x and 1.4.x
        // layouts), so a batch legitimately mixes logs of both. The ones that
        // decoded here are processed; the rest fail decode by design and are
        // handled by the sibling layout's registration.
        let _ = events_len;
        (self.callback)(&result, Arc::clone(&self.context)).await
    }
}

pub fn enabledmodule_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> EnabledModuleEventCallbackType<TExtensions>
where
    EnabledModuleResult: Clone + 'static,
    F: for<'a> Fn(Vec<EnabledModuleResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type EnabledModuleEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<EnabledModuleResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct EnabledModuleEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: EnabledModuleEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> EnabledModuleEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        EnabledModuleResult: Clone + 'static,
        F: for<'a> Fn(Vec<EnabledModuleResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: enabledmodule_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for EnabledModuleEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<EnabledModuleResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<EnabledModuleData>()
                    .ok()
                    .map(|arc| EnabledModuleResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        // Hand-edited on top of the generated output: this contract is one of
        // two registrations sharing each event's topic0 (the 1.3.x and 1.4.x
        // layouts), so a batch legitimately mixes logs of both. The ones that
        // decoded here are processed; the rest fail decode by design and are
        // handled by the sibling layout's registration.
        let _ = events_len;
        (self.callback)(&result, Arc::clone(&self.context)).await
    }
}

pub fn disabledmodule_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> DisabledModuleEventCallbackType<TExtensions>
where
    DisabledModuleResult: Clone + 'static,
    F: for<'a> Fn(Vec<DisabledModuleResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type DisabledModuleEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<DisabledModuleResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct DisabledModuleEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: DisabledModuleEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> DisabledModuleEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        DisabledModuleResult: Clone + 'static,
        F: for<'a> Fn(Vec<DisabledModuleResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: disabledmodule_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for DisabledModuleEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<DisabledModuleResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<DisabledModuleData>()
                    .ok()
                    .map(|arc| DisabledModuleResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        // Hand-edited on top of the generated output: this contract is one of
        // two registrations sharing each event's topic0 (the 1.3.x and 1.4.x
        // layouts), so a batch legitimately mixes logs of both. The ones that
        // decoded here are processed; the rest fail decode by design and are
        // handled by the sibling layout's registration.
        let _ = events_len;
        (self.callback)(&result, Arc::clone(&self.context)).await
    }
}

pub fn addedowner_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> AddedOwnerEventCallbackType<TExtensions>
where
    AddedOwnerResult: Clone + 'static,
    F: for<'a> Fn(Vec<AddedOwnerResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type AddedOwnerEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<AddedOwnerResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct AddedOwnerEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: AddedOwnerEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> AddedOwnerEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        AddedOwnerResult: Clone + 'static,
        F: for<'a> Fn(Vec<AddedOwnerResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: addedowner_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for AddedOwnerEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<AddedOwnerResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<AddedOwnerData>()
                    .ok()
                    .map(|arc| AddedOwnerResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        // Hand-edited on top of the generated output: this contract is one of
        // two registrations sharing each event's topic0 (the 1.3.x and 1.4.x
        // layouts), so a batch legitimately mixes logs of both. The ones that
        // decoded here are processed; the rest fail decode by design and are
        // handled by the sibling layout's registration.
        let _ = events_len;
        (self.callback)(&result, Arc::clone(&self.context)).await
    }
}

pub fn removedowner_handler<TExtensions, F, Fut>(
    custom_logic: F,
) -> RemovedOwnerEventCallbackType<TExtensions>
where
    RemovedOwnerResult: Clone + 'static,
    F: for<'a> Fn(Vec<RemovedOwnerResult>, Arc<EventContext<TExtensions>>) -> Fut
        + Send
        + Sync
        + 'static
        + Clone,
    Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    TExtensions: Send + Sync + 'static,
{
    Arc::new(move |results, context| {
        let custom_logic = custom_logic.clone();
        let results = results.clone();
        let context = Arc::clone(&context);
        async move { (custom_logic)(results, context).await }.boxed()
    })
}

type RemovedOwnerEventCallbackType<TExtensions> = Arc<
    dyn for<'a> Fn(
            &'a Vec<RemovedOwnerResult>,
            Arc<EventContext<TExtensions>>,
        ) -> BoxFuture<'a, EventCallbackResult<()>>
        + Send
        + Sync,
>;

pub struct RemovedOwnerEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    callback: RemovedOwnerEventCallbackType<TExtensions>,
    context: Arc<EventContext<TExtensions>>,
}

impl<TExtensions> RemovedOwnerEvent<TExtensions>
where
    TExtensions: Send + Sync + 'static,
{
    pub async fn handler<F, Fut>(closure: F, extensions: TExtensions) -> Self
    where
        RemovedOwnerResult: Clone + 'static,
        F: for<'a> Fn(Vec<RemovedOwnerResult>, Arc<EventContext<TExtensions>>) -> Fut
            + Send
            + Sync
            + 'static
            + Clone,
        Fut: Future<Output = EventCallbackResult<()>> + Send + 'static,
    {
        let (reorg_tx, _) = tokio::sync::broadcast::channel(16);
        Self {
            callback: removedowner_handler(closure),
            context: Arc::new(EventContext {
                database: get_or_init_postgres_client().await,

                extensions: Arc::new(extensions),
                reorg_tx,
            }),
        }
    }
}

#[async_trait]
impl<TExtensions> EventCallback for RemovedOwnerEvent<TExtensions>
where
    TExtensions: Send + Sync,
{
    async fn call(&self, events: Vec<EventResult>) -> EventCallbackResult<()> {
        let events_len = events.len();

        // note some can not downcast because it cant decode
        // this happens on events which failed decoding due to
        // not having the right abi for example
        // transfer events with 2 indexed topics cant decode
        // transfer events with 3 indexed topics
        let result: Vec<RemovedOwnerResult> = events
            .into_iter()
            .filter_map(|item| {
                item.decoded_data
                    .downcast::<RemovedOwnerData>()
                    .ok()
                    .map(|arc| RemovedOwnerResult {
                        event_data: (*arc).clone(),
                        tx_information: item.tx_information,
                    })
            })
            .collect();

        // Hand-edited on top of the generated output: this contract is one of
        // two registrations sharing each event's topic0 (the 1.3.x and 1.4.x
        // layouts), so a batch legitimately mixes logs of both. The ones that
        // decoded here are processed; the rest fail decode by design and are
        // handled by the sibling layout's registration.
        let _ = events_len;
        (self.callback)(&result, Arc::clone(&self.context)).await
    }
}

pub enum ChildLegacyLayoutV13EventType<TExtensions>
where
    TExtensions: 'static + Send + Sync,
{
    ExecutionSuccess(ExecutionSuccessEvent<TExtensions>),
    ExecutionFailure(ExecutionFailureEvent<TExtensions>),
    ChangedMasterCopy(ChangedMasterCopyEvent<TExtensions>),
    ChangedFallbackHandler(ChangedFallbackHandlerEvent<TExtensions>),
    ChangedGuard(ChangedGuardEvent<TExtensions>),
    ChangedModuleGuard(ChangedModuleGuardEvent<TExtensions>),
    EnabledModule(EnabledModuleEvent<TExtensions>),
    DisabledModule(DisabledModuleEvent<TExtensions>),
    AddedOwner(AddedOwnerEvent<TExtensions>),
    RemovedOwner(RemovedOwnerEvent<TExtensions>),
}

impl<TExtensions> ChildLegacyLayoutV13EventType<TExtensions>
where
    TExtensions: 'static + Send + Sync,
{
    pub fn topic_id(&self) -> &'static str {
        match self {
            ChildLegacyLayoutV13EventType::ExecutionSuccess(_) => {
                "0x442e715f626346e8c54381002da614f62bee8d27386535b2521ec8540898556e"
            }
            ChildLegacyLayoutV13EventType::ExecutionFailure(_) => {
                "0x23428b18acfb3ea64b08dc0c1d296ea9c09702c09083ca5272e64d115b687d23"
            }
            ChildLegacyLayoutV13EventType::ChangedMasterCopy(_) => {
                "0x75e41bc35ff1bf14d81d1d2f649c0084a0f974f9289c803ec9898eeec4c8d0b8"
            }
            ChildLegacyLayoutV13EventType::ChangedFallbackHandler(_) => {
                "0x5ac6c46c93c8d0e53714ba3b53db3e7c046da994313d7ed0d192028bc7c228b0"
            }
            ChildLegacyLayoutV13EventType::ChangedGuard(_) => {
                "0x1151116914515bc0891ff9047a6cb32cf902546f83066499bcf8ba33d2353fa2"
            }
            ChildLegacyLayoutV13EventType::ChangedModuleGuard(_) => {
                "0xcd1966d6be16bc0c030cc741a06c6e0efaf8d00de2c8b6a9e11827e125de8bb8"
            }
            ChildLegacyLayoutV13EventType::EnabledModule(_) => {
                "0xecdf3a3effea5783a3c4c2140e677577666428d44ed9d474a0b3a4c9943f8440"
            }
            ChildLegacyLayoutV13EventType::DisabledModule(_) => {
                "0xaab4fa2b463f581b2b32cb3b7e3b704b9ce37cc209b5fb4d77e593ace4054276"
            }
            ChildLegacyLayoutV13EventType::AddedOwner(_) => {
                "0x9465fa0c962cc76958e6373a993326400c1c94f8be2fe3a952adfa7f60b2ea26"
            }
            ChildLegacyLayoutV13EventType::RemovedOwner(_) => {
                "0xf8d49fc529812e9a7c5c50e69c20f0dccc0db8fa95c98bc58cc9a4f1c1299eaf"
            }
        }
    }

    pub fn event_name(&self) -> &'static str {
        match self {
            ChildLegacyLayoutV13EventType::ExecutionSuccess(_) => "ExecutionSuccess",
            ChildLegacyLayoutV13EventType::ExecutionFailure(_) => "ExecutionFailure",
            ChildLegacyLayoutV13EventType::ChangedMasterCopy(_) => "ChangedMasterCopy",
            ChildLegacyLayoutV13EventType::ChangedFallbackHandler(_) => "ChangedFallbackHandler",
            ChildLegacyLayoutV13EventType::ChangedGuard(_) => "ChangedGuard",
            ChildLegacyLayoutV13EventType::ChangedModuleGuard(_) => "ChangedModuleGuard",
            ChildLegacyLayoutV13EventType::EnabledModule(_) => "EnabledModule",
            ChildLegacyLayoutV13EventType::DisabledModule(_) => "DisabledModule",
            ChildLegacyLayoutV13EventType::AddedOwner(_) => "AddedOwner",
            ChildLegacyLayoutV13EventType::RemovedOwner(_) => "RemovedOwner",
        }
    }

    pub fn contract_name(&self) -> String {
        "ChildLegacyLayoutV13".to_string()
    }

    async fn get_provider(&self, network: &str) -> Arc<dyn ChainProvider> {
        get_provider_cache_for_network(network).await
    }

    fn decoder(
        &self,
        network: &str,
    ) -> Arc<dyn Fn(Vec<B256>, Bytes) -> Arc<dyn Any + Send + Sync> + Send + Sync> {
        match self {
            ChildLegacyLayoutV13EventType::ExecutionSuccess(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match ExecutionSuccessData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: ExecutionSuccessData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }

            ChildLegacyLayoutV13EventType::ExecutionFailure(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match ExecutionFailureData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: ExecutionFailureData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }

            ChildLegacyLayoutV13EventType::ChangedMasterCopy(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match ChangedMasterCopyData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: ChangedMasterCopyData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }

            ChildLegacyLayoutV13EventType::ChangedFallbackHandler(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match ChangedFallbackHandlerData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: ChangedFallbackHandlerData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }

            ChildLegacyLayoutV13EventType::ChangedGuard(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match ChangedGuardData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: ChangedGuardData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }

            ChildLegacyLayoutV13EventType::ChangedModuleGuard(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match ChangedModuleGuardData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: ChangedModuleGuardData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }

            ChildLegacyLayoutV13EventType::EnabledModule(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match EnabledModuleData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: EnabledModuleData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }

            ChildLegacyLayoutV13EventType::DisabledModule(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match DisabledModuleData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: DisabledModuleData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }

            ChildLegacyLayoutV13EventType::AddedOwner(_) => Arc::new(
                move |topics: Vec<B256>, data: Bytes| match AddedOwnerData::decode_raw_log(
                    topics,
                    &data[0..],
                ) {
                    Ok(event) => {
                        let result: AddedOwnerData = event;
                        Arc::new(result) as Arc<dyn Any + Send + Sync>
                    }
                    Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                },
            ),

            ChildLegacyLayoutV13EventType::RemovedOwner(_) => {
                Arc::new(move |topics: Vec<B256>, data: Bytes| {
                    match RemovedOwnerData::decode_raw_log(topics, &data[0..]) {
                        Ok(event) => {
                            let result: RemovedOwnerData = event;
                            Arc::new(result) as Arc<dyn Any + Send + Sync>
                        }
                        Err(error) => Arc::new(error) as Arc<dyn Any + Send + Sync>,
                    }
                })
            }
        }
    }

    pub async fn register(self, manifest_path: &PathBuf, registry: &mut EventCallbackRegistry) {
        let rindexer_yaml = read_manifest(manifest_path).expect("Failed to read rindexer.yaml");
        let topic_id = self.topic_id();
        let contract_name = self.contract_name();
        let event_name = self.event_name();

        let contract_details = rindexer_yaml
            .all_contracts()
            .iter()
            .find(|c| c.name == contract_name)
            .unwrap_or_else(|| {
                panic!(
                    "Contract {} not found please make sure its defined in the rindexer.yaml",
                    contract_name
                )
            })
            .clone();

        let index_event_in_order = contract_details
            .index_event_in_order
            .as_ref()
            .is_some_and(|vec| vec.contains(&event_name.to_string()));

        // Expect providers to have been initialized, but it's an async init so this should
        // be fast but for correctness we must await each future.
        let mut providers = HashMap::new();
        for n in contract_details.details.iter() {
            let provider: Arc<dyn ChainProvider> = self.get_provider(&n.network).await;
            providers.insert(n.network.clone(), provider);
        }

        let contract = ContractInformation {
            name: contract_details
                .before_modify_name_if_filter_readonly()
                .into_owned(),
            details: contract_details
                .details
                .iter()
                .map(|c| {
                    let provider = providers
                        .get(&c.network)
                        .expect("must have a provider")
                        .clone();

                    NetworkContract {
                        id: generate_random_id(10),
                        network: c.network.clone(),
                        cached_provider: provider.clone(),
                        block_clock: BlockClock::new(
                            rindexer_yaml.timestamps,
                            rindexer_yaml.config.timestamp_sample_rate,
                            provider.clone(),
                        ),
                        decoder: self.decoder(&c.network),
                        indexing_contract_setup: c.indexing_contract_setup(manifest_path),
                        start_block: c.start_block,
                        end_block: c.end_block,
                        disable_logs_bloom_checks: rindexer_yaml
                            .networks
                            .iter()
                            .find(|n| n.name == c.network)
                            .is_some_and(|n| n.disable_logs_bloom_checks.unwrap_or_default()),
                    }
                })
                .collect(),
            abi: contract_details.abi,
            reorg_safe_distance: contract_details.reorg_safe_distance,
        };

        let (callback, reorg_sender): (
            Arc<
                dyn Fn(Vec<EventResult>) -> BoxFuture<'static, EventCallbackResult<()>>
                    + Send
                    + Sync,
            >,
            Option<tokio::sync::broadcast::Sender<rindexer::ReorgEvent>>,
        ) = match self {
            ChildLegacyLayoutV13EventType::ExecutionSuccess(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildLegacyLayoutV13EventType::ExecutionFailure(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildLegacyLayoutV13EventType::ChangedMasterCopy(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildLegacyLayoutV13EventType::ChangedFallbackHandler(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildLegacyLayoutV13EventType::ChangedGuard(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildLegacyLayoutV13EventType::ChangedModuleGuard(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildLegacyLayoutV13EventType::EnabledModule(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildLegacyLayoutV13EventType::DisabledModule(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildLegacyLayoutV13EventType::AddedOwner(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }

            ChildLegacyLayoutV13EventType::RemovedOwner(event) => {
                let reorg_sender = Some(event.context.reorg_tx.clone());
                let event = Arc::new(event);
                let callback = Arc::new(move |result| {
                    let event = Arc::clone(&event);
                    async move { event.call(result).await }.boxed()
                });
                (callback, reorg_sender)
            }
        };

        registry.register_event(EventCallbackRegistryInformation {
            id: generate_random_id(10),
            indexer_name: "safe-indexer".to_string(),
            event_name: event_name.to_string(),
            index_event_in_order,
            topic_id: topic_id.parse::<B256>().unwrap(),
            contract,
            callback,
            tables: Arc::new(vec![]),
            reorg_sender,
            streams_clients: Arc::new(None),
            providers: Arc::new(providers),
            constants: Arc::new(rindexer_yaml.constants.clone()),
            multicall_addresses: Arc::new(
                rindexer_yaml
                    .networks
                    .iter()
                    .map(|n| (n.name.clone(), n.multicall3_address.clone()))
                    .collect(),
            ),
        });
    }
}
