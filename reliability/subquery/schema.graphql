type TransferEvent @entity {
  id: ID!
  blockNumber: Int!
  logIndex: BigInt!
  from: String!
  to: String!
  value: BigInt!
  timestamp: Int!
}

type TokenMetadata @entity {
  id: ID!
  blockNumber: Int!
  logIndex: BigInt!
  symbol: String!
  name: String!
}
