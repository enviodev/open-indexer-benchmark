# Manifests the drivers write per run: the block range has to be baked into
# each tool's configuration, and most scenarios have no end block at all.
envio/config.yaml
subgraph/subgraph.yaml
rindexer/rindexer.yaml
subquery/project.yaml
subquery/.env

# Build and cache directories.
**/node_modules/
**/generated/
**/dist/
**/build/
envio/.envio
ponder/.ponder
subquery/.data
subquery/src/types/

# Generated by `envio codegen`.
envio/envio-env.d.ts
